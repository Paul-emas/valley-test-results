import type { Metadata } from "next";
import localFont from "next/font/local";
import "./globals.css";

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

const ppNeueMontreal = localFont({
  src: [
    {
      path: "../../public/fonts/pp_neue/ppneuemontreal-bold.otf",
      weight: "700",
      style: "normal",
    },
    {
      path: "../../public/fonts/pp_neue/ppneuemontreal-medium.otf",
      weight: "500",
      style: "normal",
    },
    {
      path: "../../public/fonts/pp_neue/ppneuemontreal-book.otf",
      weight: "400",
      style: "normal",
    },
  ],
});

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${ppNeueMontreal.className} antialiased`}>
        {children}
      </body>
    </html>
  );
}
